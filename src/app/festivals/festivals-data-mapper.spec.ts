import { FestivalsDataMapper } from './festivals-data-mapper';

describe('FestivalsDataMapper', () => {
  it('should create an instance', () => {
    expect(new FestivalsDataMapper()).toBeTruthy();
  });
});
